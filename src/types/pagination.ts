/**
 * @typedef module:types.Pagination
 * @type {object}
 * @property {number} limit
 * @property {number} page
 * @property {number} offset
 */
export interface Pagination {
    limit: number;
    page: number;
    offset: number;
}
